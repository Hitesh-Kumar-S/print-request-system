# Application name
spring.application.name=printApp

# ===== MySQL Database Configuration (Local only) =====
spring.datasource.url=jdbc:mysql://localhost:3306/Print_Management
spring.datasource.username=root
spring.datasource.password=Jr_Shk@life
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Thymeleaf (disable cache during development)
spring.thymeleaf.cache=false

# Server port (default 8080)
server.port=8080

